import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#D4AF37",
      contrastText: "#fff"
    },
    background: {
      default: "#FAFFCC"
    }
  },
})

export default theme;
